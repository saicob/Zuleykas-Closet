// users UI removed
document.addEventListener('DOMContentLoaded', ()=>{
  const el = document.getElementById('users-root')
  if(el) el.innerHTML = '<p>El sistema de usuarios ha sido eliminado. Reimpleméntalo desde cero cuando estés listo.</p>'
})
